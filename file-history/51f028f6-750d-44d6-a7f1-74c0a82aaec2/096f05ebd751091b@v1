# Plan for converting Word docs to PDF

## Context
The user wants every Word document in `/Users/my/Desktop/RA/杰青/2026Zhan/本子附件/Word` duplicated as a same-named PDF inside `/Users/my/Desktop/RA/杰青/2026Zhan/本子附件/PDF` without altering the original `.docx` files. This is a filesystem operation rather than a code change, but it touches multiple files in the document directories and therefore needs a structured, reversible plan.

## Approach
1. Verify the list of `.docx` files that need conversion and make sure the destination directory exists, creating `/Users/my/Desktop/RA/杰青/2026Zhan/本子附件/PDF` if necessary without touching the Word files.
2. For each `.docx`, run a reliable Word-to-PDF converter (e.g., `libreoffice --headless --convert-to pdf --outdir <PDF dir> <docx>`) so that the generated PDF mirrors the source filename and lands in the PDF directory.
3. Confirm the conversion succeeded by comparing the count of `.docx` files with the number of newly written `.pdf` files, and inspect a couple of samples to ensure the content was preserved.
4. Leave the Word files untouched throughout; only the `PDF` directory will receive new files.

## Scripts/Commands
- Use `mkdir -p '/Users/my/Desktop/RA/杰青/2026Zhan/本子附件/PDF'` to ensure the output directory exists.
- Use `libreoffice --headless --convert-to pdf --outdir '/Users/my/Desktop/RA/杰青/2026Zhan/本子附件/PDF' '<each source file>'` to perform the conversion.

## Files affected
- Input: `/Users/my/Desktop/RA/杰青/2026Zhan/本子附件/Word/*.docx`
- Output: `/Users/my/Desktop/RA/杰青/2026Zhan/本子附件/PDF/*.pdf`

## Verification
- After the conversion, run `ls` or similar to list the PDFs and ensure every source `.docx` has a corresponding `.pdf` with the same basename.
- Manually open a couple of the PDFs to confirm their integrity (content matches the original Word document).